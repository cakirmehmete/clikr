import { createMuiTheme } from '@material-ui/core/styles';

const typeographytheme = createMuiTheme({
    typography: {
        useNextVariants: true,
        suppressDeprecationWarnings: true
    }
});
export default typeographytheme;